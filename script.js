// Example: Alert on button click
function showAlert() {
    alert('Hello, this is a simple alert!');
}

// Add event listener to a button (if you add one later)
document.querySelector('button').addEventListener('click', showAlert);
